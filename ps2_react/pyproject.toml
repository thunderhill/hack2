[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ps2-pipeline-anomaly-react"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "httpx>=0.27.0",
]
