[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ps5-infra-change-explainer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40.0",
    "streamlit>=1.35.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    "chromadb>=0.4.0",
    "mcp[cli]>=1.0.0",
]
