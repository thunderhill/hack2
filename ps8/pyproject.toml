[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ps8-incident-report"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40.0",
    "streamlit>=1.35.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
]
